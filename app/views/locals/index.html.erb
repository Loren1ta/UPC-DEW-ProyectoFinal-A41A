<h1>Listing locals</h1>

<table>
  <tr>
    <th>Namelocal</th>
    <th>Address</th>
    <th>District</th>
    <th>Addressgooglemap</th>
    <th>Longitude</th>
    <th>Phone</th>
    <th>State</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @locals.each do |local| %>
  <tr>
    <td><%= local.nameLocal %></td>
    <td><%= local.address %></td>
    <td><%= local.district %></td>
    <td><%= local.addressGoogleMap %></td>
    <td><%= local.longitude %></td>
    <td><%= local.phone %></td>
    <td><%= local.state %></td>
    <td><%= link_to 'Show', local %></td>
    <td><%= link_to 'Edit', edit_local_path(local) %></td>
    <td><%= link_to 'Destroy', local, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Local', new_local_path %>
